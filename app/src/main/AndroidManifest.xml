<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pbilbd">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.STORAGE" /> <!-- uses-feature -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- uses-feature -->
    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/pbi_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pbi_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PBIBD"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".activities.signup.SignUpActivity"
            android:exported="true"
            android:theme="@style/Theme.PBIBD.NoActionBar" />
        <activity
            android:name=".activities.signin.SignInActivity"
            android:exported="true"
            android:theme="@style/Theme.PBIBD.NoActionBar" />
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PBIBD.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PBIBD.NoActionBar" />
    </application>

</manifest>